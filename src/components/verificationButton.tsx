import React from "react";

const VerificationButton = () => {
  return (
    <button className="flex w-[6rem] text-text-main h-[2rem] rounded-[6.25rem] bg-default text-body-sm font-semibold justify-center items-center">
      과거 지표 검증하기
    </button>
  );
};

export default VerificationButton;
